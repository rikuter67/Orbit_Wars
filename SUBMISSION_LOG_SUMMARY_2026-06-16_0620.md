# 6/16 06:20 以降 実験要約（提出ログ短縮版）

対象: `SUBMISSION_LOG.md` の  
「`2026-06-16 06:20-06:44 JST (Producer mapgate refinement, 提出なし)` 以降」

この期間は、**提出なし**の候補が大量に積まれたが、Live提出ルールを壊さない条件にすぐ詰まった期間。  
結論は「**現行採用候補（h19+2P Producer-gate）維持**」に集約。

---

## 0) 結論だけ（先に読む）

1. `h19` ベースの安定性は維持できたが、**Producer対策を強くすると h19-like 対策で崩れる**傾向が再三確認された。  
2. `Producer対策` と `h19 self-like` と `oldv2耐性` の3軸を同時に改善できる単一差分は、この期間は見つからなかった。  
3. map-gate・スイッチ系は「効果が見えるが誤反応が増えやすい」、softmod系は「h19安定性改善はあるがProducer優位化まで到達しない」が共通。  
4. 機能的な実験としては `orbit_path_eval.py` を追加し、bundle/selector向け定量化が改善された（再現/検証フローは強化）。
5. 最後の提出候補判定は、**latest2収束 + 保留なし + 3h間隔 + 事前検証**を外さないことが不変ルール。

---

## 1) 主要テーマ別 要約

### A. 初期マップやパラメータでの大きな分岐切替（06:20-09:30）

- 試行: map-gate（lowfast系4種）、lossgate、public補完、early conserve/aggressive
- 結果:
  - 初期特徴での分岐は再現性が不安定（h19側を落としやすい）
  - Producerへの有効性は限定的
  - 早期行動制御（抑え/強化）も、h19側の自己対決を大きく改善しない
- 判定: **未採用（提出なし）**

### B. 予測型/検出型（action-based）

- 試行: opponent planner 予測で設定切替（Producer-like判定）、strict条件、softmod（attack/avoid係数スイープ）
- 結果:
  - Producer傾向はある程度検出できる
  - ただし設定を切る/変えると `h19/h19-like` 対戦で誤反応しやすく崩れる
  - softmodは“安全寄せ”には寄与するが、Producerに対する明確な優位化には未到達
- 判定: **全体として未採用、研究枠維持**

### C. multisize / oldv2対策（09:00-13:00）

- 試行: oldv2マルチサイズ、初手ゲート、統合multisize、nonmine075系、margin18_floor20系
- 結果:
  - `m05_100` 系は oldv2 的には有望
  - しかし Producer/h19-like の共通ラインで不安定
  - `nonmine075` は単体指標は強いが、h19-like が弱くなる
- 判定: **Live採用保留（未採用）**

### D. selector 系 / 再現系評価の強化（13:40-19:40）

- 試行: selector再試行、path evaluator追加、パス実行モードによる再チェック
- 結果:
  - selector や path ベースの比較環境は実戦再現性改善に有効
  - ただし最適な切り替え条件を満たす「実投稿可能候補」はなし
- 判定: **現状は評価基盤強化のみ有効、提出候補は維持**

---

## 2) ここまでの採択判断（要点）

- 「収束しない/収束条件未確認」なら提出禁止。
- 「oldv2」や「h19-like」相手での崩れがあるものは却下。
- 「Producerに強い」だけでは不採用。  
  **`h19` + `Producer` + `oldv2` を同時に許容できること**が最終条件。
- 現状は `6/16 06:47` の `highfast85 Producer-gate` を継続運用。

---

## 3) 生ログ（元データ）との対応

元の全文は以下に残存:
- `SUBMISSION_LOG_AFTER_2026-06-16_0620.md`
- 生ログ本文: `SUBMISSION_LOG.md`

必要ならこの要約を、さらに以下の形式でも作れる:
- 時系列 1行ダイジェスト表（時間帯／候補種別／結果）
- 各段階での `勝敗ベスト` だけを抽出したCSV
- 「提出判定ログ」だけの短いタイムライン（1ページ）
